# Phase 1 dependencies for IA/ (TNFR Microscope on SmolLM2)
# Install torch first with the matching CUDA wheel:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch>=2.4
# Then:
#   pip install -r IA/requirements-phase1.txt

transformers>=4.45,<5
accelerate>=0.34
datasets>=3.0
huggingface-hub>=0.25
numpy>=1.26
scipy>=1.11
networkx>=3.0
pandas>=2.0
matplotlib>=3.8
tqdm>=4.66
# tnfr is provided by the parent repo via `pip install -e .` from the workspace root.
