# Reports

This folder will hold the analysis outputs of the IA/ phase runs:

- `phase1_microscope.csv` — raw per-problem telemetry from `experiments/02_microscope_gsm8k.py`.
- `phase1_microscope.md` — Mann-Whitney U tests + plots + decision (continue / pivot).

Reports are committed so the standalone repo carries its evidence trail.
