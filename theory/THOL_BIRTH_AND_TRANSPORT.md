# Causal THOL birth and an admitted transport connection

**Status:** Exact finite acceleration and edge-reset identities with bounded
canonical runtime controls. Persistent self-restoring localization remains open.
**Research links:** B2.d.6/O3.a, S3, S8, S9, S10 and S16.

## 1. Deriving a birth trigger from actual nodal evolution

Consider a fixed simple unit cycle with uniform held capacity `nu>0`, scalar
EPI `x`, and a regular phase twist strictly inside the effective U3 gate
and the neighbor semicircle boundary. Its phase, capacity and topology
pressure gradients vanish. With the existing fully normalized EPI weight
`e=w_epi>0`, define `c=e*nu` and `L=I-W/2`. The canonical nodal equation is

$$
\dot x=-cLx.
$$

Two successive exact refreshed Euler segments of positive durations
`h1,h2` give

$$
x_1=(I-ch_1L)x_0,\qquad
x_2=(I-ch_2L)x_1.
$$

Their secants satisfy `s1=-c*L*x0` and `s2=-c*L*x1`. The same three-point
acceleration used by
[`compute_d2epi_dt2`](../src/tnfr/operators/nodal_equation.py) therefore has
the exact reference identity

$$
\widehat A=\frac{2(s_2-s_1)}{h_1+h_2}
=\frac{2h_1c^2}{h_1+h_2}L^2x_0.
$$

The first duration occurs in the numerator because the second refreshed
rate is evaluated at the endpoint of the first Euler segment. This is
a finite solver identity, not an asserted exact second derivative of
the continuous exponential trajectory. The sufficient convex conditions
`c*h_j<=1` keep all EPI endpoints in the convex hull of the initial field;
clipping and runtime arithmetic still require observed residuals.

On an even cycle let `v_i=(-1)^i`, so `Lv=2v`, and prepare
`x0=b*1+a*v`. Then

$$
\widehat A_i=
\frac{8h_1c^2a}{h_1+h_2}v_i.
$$

The default THOL magnitude threshold is crossed when

$$
\frac{8h_1(e\nu)^2|a|}{h_1+h_2}>\tau.
$$

This is a derived sufficient preparation condition. It leaves the
existing threshold and operator coefficients unchanged and supplies no
independent acceleration law. Runtime admission additionally requires
the actual operator grammar and parent domain checks.

## 2. A bounded preparation with the unchanged default threshold

The finite C8 preparation uses alternating EPI levels `2` and `1/2`,
unit capacity and winding one. Its scalar chart bounds are explicitly
`[-4,4]`; these are declared experiment bounds rather than the default
`[-1,1]` bounds. Here `b=5/4`, `a=3/4`. Two refreshed segments of duration
`h1=h2=1/4` give

$$
\widehat A_i=3e^2v_i,\qquad
x_2=\frac54\mathbf1+
\frac34(1-e/2)^2v.
$$

Using the materialized default normalized coefficient
`e=0.18315345241335917`, the positive trigger is approximately
`0.1006355614`, above the unchanged default `tau=0.1`. The Euler
coefficients are below `1/2`, so this exact reference remains within the
initial interval `[1/2,2]` and inside the declared chart bounds. The
threshold margin is finite; the runtime comparison records its actual
three samples and arithmetic residual rather than assuming equality
with the symbolic calculation.

Actual Coherence/Dissonance preparation supplies the required operator
context. Canonical pressure is refreshed before the two physical
segments. The three samples are consequently generated by those executed
segments, rather than inserted as a desired threshold-crossing history.
Choosing one high-EPI parent for THOL remains an explicit target policy;
it is not a claim that the location emerged by spontaneous symmetry
breaking.

## 3. Birth creates a node; Coupling must supply an actual edge

The distinction established in
[THOL pressure feedback](THOL_PRESSURE_FEEDBACK.md) remains essential:
public THOL creates an initially isolated child with a triad and hierarchy
metadata. The node does not enter its parent's neighbor-pressure walk
through the metadata alone. Its initial pressure is zero, capacity is
the existing `0.95` multiple of the parent capacity, and phase is inherited.
Its EPI is the bounded output of the existing metabolic amplitude map.

An actual graph-backed Coupling operation can subsequently consider the
child as a functional-link candidate. The candidate inventory must
include the new node; a retained prebirth sample of graph nodes need
not do so. The candidate must pass both the U3 phase condition and the
existing compatibility threshold. There is no automatic parent-child
edge implied by THOL's hierarchy.

The parent target must already have a compatible neighbor when the
hard U3 gate is checked, before functional candidates are considered.
Targeting the isolated child, or including it as a target in an all-node
stage, fails that gate despite its inherited phase alignment. The
successful route targets the parent with its existing cycle neighbors.

The
[Coupling proposal kernel](../src/tnfr/operators/_coupling_stage_kernel.py)
computes link weight from final merged phases and the snapshot EPI/sense
inputs. In its scalar regime this has the form

$$
\omega=\tfrac12\left(1-\frac{|\operatorname{wrap}
(\phi_i-\phi_j)|}{\pi}\right)
+\tfrac14\left(1-\frac{|x_i-x_j|}
{|x_i|+|x_j|+\epsilon}\right)
+\tfrac14(1-|S_i-S_j|),
$$

with the existing epsilon and sense-index conventions. The implementation
evaluates the EPI ratio with scaling to preserve its numeric domain.
The default compatibility threshold is `pi/(pi+1)`. The resulting
positive conductance is an operator-derived weight, not an independently
fitted parent-child coupling strength.
Inherited equal phase alone is insufficient: the EPI and sense-index
terms can still place the candidate below the compatibility threshold.

Parent-target UM computes its phase, capacity and direct pressure
proposals from the old compatible neighborhood before adding the
functional edge. The newly considered child is therefore not silently
included in that same call's original neighbor capacity mean. Default
bidirectional UM can also change the parent's old neighbors' phases.
The full before/after triads and the subsequent pressure refresh must
be observed; a pure edge-only model does not describe every UM channel.

## 4. What the new edge changes in the canonical pressure

Keep scalar EPI fixed across a new undirected edge `i-j` with positive
weight `omega`. For endpoint `i`, let `d_i` be its old weighted strength
and `mx_i` its old weighted EPI neighbor mean. If `d_i>0`, the change of
the normalized EPI gradient is

$$
g^+_{x,i}-g^-_{x,i}
=\frac{\omega}{d_i+\omega}(x_j-mx_i).
$$

For a former isolate, the new single-neighbor gradient is instead
`g_x^+=x_j-x_i`, independently of the strictly positive incident weight.
This is normalization of the actual adjacency, not infinite coupling.

The capacity channel uses the **unweighted unique neighbor support**.
With old neighbor count `k_i>0` and mean capacity `bar_nu_i`, its edge-only
change is

$$
g^+_{\nu,i}-g^-_{\nu,i}
=\frac{\nu_j-\bar\nu_i}{k_i+1}.
$$

A former isolate gets `nu_j-nu_i`. Zero-weight support edges can affect
capacity, phase and topology while contributing no EPI conductance;
the weighted EPI walk cannot replace those other channel conventions.
If UM also changes capacity, evaluate these support-only identities on
one declared held triad and account for the capacity write separately.

For the actual unit-cycle parent joined to one isolated child, denote
the parent's EPI by `H`, its old two neighbors' common EPI by `J`, and
the child's EPI by `z`. For the ideal closed-form reference with parent
capacity one and child capacity `19/20`, the endpoint gradients are

$$
g_{x,p}^+=\frac{2J+\omega z}{2+\omega}-H,\qquad
g_{x,c}^+=H-z,\qquad
g_{\nu,p}^+=-\frac1{60},\qquad
g_{\nu,c}^+=\frac1{20}.
$$

The symmetric parent-target phase update leaves the child and parent
phases equal in the exact preparation. Their phase pressures vanish,
so the refreshed child pressure is

$$
p_c=e(H-z)+\frac{w_{\nu}}{20}.
$$

The production damping coefficient is binary64 `0.95`, not the exact
rational `19/20`. For its exact represented capacity difference
`delta=1-Fraction(float(0.95))`, replace `-1/60` and `1/20` by
`-delta/3` and `delta`, respectively, including the pressure formula.

The default topology weight is zero. For a configured nonzero topology
channel, the new degree-neighbor gradients are `-4/3` at the parent,
`2` at the child, `1/2` at each old neighbor of the parent, and zero
elsewhere. These formulas use a simple unit undirected cycle; arbitrary
weighted or parallel-edge graphs require the implementation's actual
degree and unique-support conventions.

The old neighbors can acquire nonzero phase pressure after bidirectional
UM. The newly connected whole graph therefore need not follow pure EPI
diffusion, even when the parent and child formulas above simplify.
Likewise the previous cycle coordinate `x+(w_vf/e)*nu` does not generally
cancel pressure once weighted EPI transport and unweighted capacity
transport differ.

## 5. Connection has an exact energy reset, not a conservation claim

On fixed ordered support, let `W` be symmetric conductance, `D` its
strength diagonal and `B=D-W`. The existing EPI Dirichlet diagnostic is

$$
F_W(x)=\tfrac12x^\top Bx
=\tfrac12\sum_{\{i,j\}}w_{ij}(x_i-x_j)^2.
$$

Compare the graph **after birth but before UM** with the graph after
the edge commit, including the child in both node orders. If UM leaves
EPI unchanged, its exact edge reset is

$$
F_{W^+}(x)-F_{W^-}(x)
=\tfrac12\sum_{\{i,j\}}(w^+_{ij}-w^-_{ij})(x_i-x_j)^2.
$$

For the single new parent-child edge this is
`omega*(H-z)^2/2>=0`. The change reflects a new conductance in the
diagnostic while EPI is fixed; it is not proof of conserved energy or
new EPI creation. It is also not a birth-energy theorem across different
node supports.

After the edge commit, a held-support nodal rate `r=diag(nu)*p` has

$$
\dot F=(Bx)^\top r.
$$

The EPI-channel contribution alone is
`-e*(Bx)^T*diag(nu_i/d_i)*(Bx)<=0`, with zero mobility at isolates.
The capacity, phase and topology terms keep their own signs; a full
multichannel decrease does not follow from the EPI term. A subsequent
exact held-input Euler interval has the finite identity

$$
F(x+h r)-F(x)=h(Bx)^\top r+\tfrac12h^2r^\top Br.
$$

These identities reuse the existing
[conductance and diffusion geometry](../src/tnfr/physics/structural_diffusion.py).
They measure how a created child becomes dynamically connected and how
the changed structure enters the nodal channels.

## 6. A precise boundary for the connected pattern's next evolution

Freeze the connected post-UM support, capacities and phases before any
later Silence or structural event. Assume positive capacities and fixed
symmetric conductance on a connected graph. With `D=diag(d_i)`, define
the held non-EPI pressure contribution

$$
F=w_{\nu}g_{\nu}+w_{\phi}g_{\phi}+w_{\mathrm{topo}}g_{\mathrm{topo}}.
$$

The refreshed canonical model, while its scalar chart remains valid, is

$$
\dot x=\operatorname{diag}(\nu)(-eD^{-1}Bx+F).
$$

A zero-pressure equilibrium requires `e*B*x=D*F`. Since the symmetric
connected Laplacian has kernel `span{1}`, the necessary and sufficient
compatibility condition is

$$
\mathbf1^\top DF=\sum_i d_iF_i=0.
$$

This is an exact balance condition for the actual channel conventions.
It is not implied by each channel separately resembling neighbor
averaging: the EPI channel's stationary weights are the conductance
strengths, while the capacity channel uses unweighted support.

In the ideal reflected C8 preparation, the parent-target bidirectional
UM preserves reflection symmetry of the graph and capacities. Its phase
gradient is antisymmetric under that reflection, with zero at the
fixed nodes, so `sum(d_i*g_phi_i)=0` inside the strict chart. With the
default zero topology weight and ideal child capacity `19/20`, the
capacity contribution gives

$$
\sum_i d_iF_i
=w_{\nu}\left[-\frac{2+\omega}{60}+\frac{\omega}{20}\right]
=\frac{w_{\nu}(\omega-1)}{30}<0.
$$

Here the observed link has `0<omega<1`. Consequently this frozen ideal
structure has no zero-pressure stationary EPI. Reflection cancellation
is an exact-model statement; the actual binary64 weighted phase
contribution must be measured as a residual.
For the exact represented capacity difference `delta` above, the
weighted capacity contribution is instead
`2*w_vf*(omega-1)*delta/3`, with the same strictly negative sign.

The captured connected state confirms that sign: the weighted capacity
pressure is approximately `-0.0003191519727040398`, while the weighted
stored pressure is `-0.0003191519727039513`. Their weighted phase and
arithmetic remainder is approximately `8.86e-17`. The corresponding
instantaneous represented nodal mean rate is `-1.8015381161580725e-5`.
These quantities are read before the subsequent flow and final SHA;
they are not observations of a future limiting drift.

There is a different conditional possibility. Define

$$
\bar v=\frac{\sum_i d_iF_i}{\sum_i d_i/\nu_i}.
$$

The weighted EPI mean has derivative `bar_v`. The equation

$$
eBz=DF-\bar v\,(d_i/\nu_i)_i
$$

is compatible and determines a relative profile `z` up to a constant.
Then `x(t)=bar_v*t*1+z` solves the held model. Subtracting that solution
leaves homogeneous fixed-capacity EPI diffusion, so relative deviations
decay on a connected graph. This conclusion concerns an unrestricted
scalar chart with fixed coefficients. A nonzero mean drift eventually
encounters finite EPI bounds; it gives no asymptotic claim for a clipped
runtime or for a later UM/SHA policy.

The next bounded question is therefore to distinguish a held-support
relative profile with mean drift from restoration under further admitted
structural operations. The [frozen-support follow-up](FORCED_SUPPORT_BALANCE.md)
now implements the exact relative-profile reference, nonlinear forcing
read-out and finite comparison. A repeated-policy theorem remains open. Neither
birth, one successful connection nor this compatibility calculation
establishes a persistent self-restoring localized entity.

## 7. Shared observations and falsification criteria

[`support_transport.py`](../src/tnfr/physics/support_transport.py) centralizes
the finite reference budgets on the engine's materialized conductance:

- `observe_support_transport` reads weighted EPI transport, unweighted
  capacity and topology gradients, Dirichlet energy, and the exact product
  of represented capacity with **stored** pressure. It does not refresh
  pressure or identify its remainder as a canonical phase contribution.
- `observe_support_transport_reset` verifies the same-EPI conductance
  reset on one aligned node set. The child must already be present in
  both observations; birth itself is outside that identity.
- `observe_support_transport_euler` separates the held-input drift,
  quadratic Euler term and observed endpoint defect on fixed conductance
  and capacity. It recomputes detached state fields before checking the
  identity; publicly constructible observations carry no causal seal.

These APIs reuse the existing conductance reader and scalar EPI chart.
Effective binary64 weights and state values are treated as exact real
coefficients after materialization. A zero budget residual proves the
displayed algebra for those inputs, not that the caller executed a
particular solver, refreshed pressure or preserved a future trajectory.

The bounded campaign in
[`thol_birth_transport.py`](../benchmarks/thol_birth_transport.py) keeps
the complete parent-target word `IL, OZ, THOL, UM, SHA`, checks both
whole-word validators and the live gate before each public application,
and records the actual graph mutations. Its birth samples come from
one executor-owned refreshed physical partition. Its later fixed-support
Euler intervals have explicit canonical refreshes at the captured live
endpoints; the final parent SHA follows the measured transport endpoint.
Atomicity belongs to each executor invocation, rather than to this
entire changing-node orchestration.

The recorded attached case has the following finite results; displayed
decimals summarize the captured binary64 states and exact rational budgets.

| Quantity | Observed value |
|----------|----------------|
| Physical acceleration before THOL | `0.10063556139279584` |
| Default birth threshold | `0.1` |
| Committed parent-child conductance | `0.8357695998168526` |
| Refreshed connected child pressure | `0.2743391213451684` |
| Child EPI at times `0.5, 0.75, 1.0` | `0.3869753, 0.4521308, 0.5118328` |
| Same-EPI support energy reset | `+0.9177477068340985` |
| Two subsequent flow energy changes | `-1.2273590449, -1.0125654108` |

The support reset and both Euler energy decompositions have exactly zero
rational identity residual. The largest captured attached Euler state
defect is `15/144115188075855872`, approximately `1.04e-16`; it is retained
in the energy accounting rather than set to zero. These are finite
observations, not uniform numerical-error bounds.
The observed acceleration differs from the exact represented reference
`3*e^2` by approximately `-2.04e-15`. Full states, fractions and source
provenance are retained locally in
`artifacts/research/thol_birth_transport.json`. Regenerate this ignored
artifact with `python benchmarks/thol_birth_transport.py`.

The paired controls disable functional linking or retain the prebirth
candidate sample. They preserve the same birth preparation while
testing whether an actual transport edge is needed. A successful child
creation must not be reported as successful coupling in either control.
Both controls keep the child pressure zero and its EPI exactly at the
birth value through the measured intervals. The 22 independent exact
observer tests are in
[`test_support_transport.py`](../tests/physics/test_support_transport.py);
the 17 bounded runtime checks are in the
[runtime test suite](../tests/physics/test_thol_birth_transport_runtime.py).

The proposed mechanism would be contradicted by a claimed causal birth
whose live history fails the unchanged acceleration threshold, a claimed
connection absent from the actual UM commit, or child transport without
a pressure-producing adjacency change under the stated isolated control.
A disagreement with the exact edge/Euler identities requires checking
the captured state, support and numerical defect rather than inferring
new dynamics. Persistent restoration requires additional evidence after
the finite birth/attachment sequence and must distinguish the mean-drift
obstruction in Section 6 from a zero-pressure localized equilibrium.
