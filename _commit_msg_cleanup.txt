Deep repo cleanup: remove scratch/dead files, fix broken entry point

Removes stale scratch, dead modules, and foreign data that accumulated at
the repository root and in the tree, and repairs a dangling console-script
entry point left behind by an earlier cleanup.

Removed (tracked):
- query_args.json, query_fields.json, query_fields_deep.json, query_full.json,
  types.json — foreign GraphQL scratch (a DeFi/trading API schema: Position,
  Market, BTC), unrelated to the TNFR engine and referenced by nothing.
- debug_cuda_backend.py — root-level CUDA debug scratch, unreferenced.
- tests/mathematics/_backup_test_riemann_spectral_proof_v1.py — backup test
  (leading underscore => not even collected by pytest), unreferenced.
- src/tnfr/cli/validate.py — empty 0-byte dead module.
- .github/todo_list.json — stale completed-task tracker, no workflow uses it.

Fixed:
- pyproject.toml: drop the dangling console script
  `tnfr-validate = "tnfr.cli.validate:main"` that pointed at the now-removed
  empty module (it had no `main` and would fail on invocation). The live
  entry points `tnfr` and `tnfr-is-prime` are intact.

Also removed untracked temp_*.log debug logs from the working tree.

Deliberately kept after investigation: src/tnfr/utils/numeric.py (marked
deprecated but has 6 live importers), validation/__init__.py compat shims
(load-bearing), and the src/tnfr/<module>/README.md doc directories (an
intentional convention alongside each <module>.py, not shadow packages).

Full suite: 2039 passed, 2 skipped. Package imports verified; pyproject TOML
validated.
